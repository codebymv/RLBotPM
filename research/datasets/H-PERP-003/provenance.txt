H-PERP-003 — OKX hedged-carry research data

Pre-registered design: ../../06_backtest_design_H-PERP-003.md

Last pull (UTC): see PROVENANCE_PULL.md in this folder (generated by fetch_hedged_panel.py).

Instruments (frozen in 06):
- Funding + perp mark 1H: OKX BTC-USDT-SWAP (public funding-rate-history + history-mark-price-candles, bar 1H).
- Spot 1H closes: OKX BTC-USDT (history-candles, bar 1H).

Output artifact:
- btc_hedged_panel_okx.csv — one row per funding event; align_ok when both 1H legs contain fundingTime with skew-to-boundary ≤ 60s each.

Funding sign (short, verify against OKX docs at execution time):
- Positive published fundingRate ⇒ long pays short ⇒ short receives +rate × notional in the usual USDT-linear convention. Backtest uses fr at the end-of-interval row as b in 06 §3.
